version: "3.9"

services:
  n8n:
    image: n8nio/n8n:latest
    container_name: foodlink-n8n

    ports:
      - "5678:5678"

    environment:
      - TZ=Asia/Kolkata
      - N8N_SECURE_COOKIE=false

    volumes:
      - ./n8n_data:/home/node/.n8n

    restart: unless-stopped